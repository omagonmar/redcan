LAMBDA			05-Dic-88	(Pedro Gigoux)

This directory contains the code of the task that computes and prints the
pixel value and corresponding wavlength value for all pixels in a list
of one dimensional images. The files are:

mkpkg		mkpkh file

t_lambda.x	main procedure task
