# Root help directory for the SAO radial velocity package.  This dummy
# package is necessary in order to have `rvsao' appear as a module in some
# package, so that the user can type "help rvsao" (with the `rvsao' given
# as a task).

_r2rvsao		pkg = r2rvsao$lib/rootr2rvsao.hd
