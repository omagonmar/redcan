# Global MKPKG definitions for the Gemini package.

$set XFLAGS     = "$(XFLAGS) -p gemini -p noao -p tables -p fitsutil"
$set XVFLAGS    = "$(XVFLAGS) -p gemini -p noao -p tables -p fitsutil"
$set LFLAGS     = "$(LFLAGS) -p gemini -p noao -p tables -p fitsutil"
